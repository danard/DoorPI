<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:mm="clr-namespace:MediaManager.Forms;assembly=MediaManager.Forms" 
             xmlns:viewmodels="clr-namespace:DoorPIApp.ViewModels" 
             x:DataType="viewmodels:GrabarEntradaViewModel"
             x:Class="DoorPIApp.Views.GrabarEntradaView"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <viewmodels:GrabarEntradaViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding UrlTransmision}" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" FontSize="Large" TextColor="Blue"/>
            
            <!--Video player-->
            <mm:VideoView VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" 
                          ShowControls="True" BackgroundColor="Black" VideoAspect="AspectFit" />
            
            <Button Text="Ver" Command="{Binding VerTransmisionCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>